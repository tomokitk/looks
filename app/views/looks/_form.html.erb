<%= form_with(model: look, local: true) do |form| %>
  <% if look.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(look.errors.count, "error") %> prohibited this look from being saved:</h2>

      <ul>
      <% look.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :look_name %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.text_field :gender, id: :look_gender %>
  </div>

  <div class="field">
    <%= form.label :uni %>
    <%= form.text_field :uni, id: :look_uni %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
